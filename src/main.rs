// mod print;
// mod vars;
// mod types;
// mod strings;
// mod tuples;
// mod arrays;
// mod vectors;
// mod conditionals;
// mod loops;
// mod functions;
// mod pointer_ref;
// mod structs;
// mod enums;
mod cli;

fn main() {
    // print::run();
    // vars::run();
    // types::run();
    // strings::run();
    // tuples::run();
    // arrays::run();
    // vectors::run();
    // conditionals::run();
    // loops::run();
    // functions::run();
    // pointer_ref::run();
    // structs::run();
    // enums::run();
    cli::run();
}

// To build the project for prodution use the: cargo build --release
// And the executable used in production will be located in the /target/release folder
// Use cargo run to build and automatically execute the project

// The mob statement import file/modules from other places, and include then in the current file

// REMEMBER: Variables are immutabe by default

// Rust is a Systems proggraming language

// You can pass arguments through the command line. E.g: When you run cargo run, there is this possibility
